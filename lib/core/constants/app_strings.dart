class AppStrings {
  static const appName = 'Book Cart';
  static const sellTitle = 'List a Book for Sale';
  static const sellSubtitle =
      'Tap the book image area, add your details manually, then post the listing.';

  const AppStrings._();
}
